# Editor
*.swp
*~
.vscode/

# macOS
.DS_Store

# chezmoi local config (machine-specific)
.chezmoi.toml

# Private / local env — never commit secrets (TOML only)
.env.toml
.env.toml.local
*.env.toml.local
config/private/

# Python
__pycache__/
*.pyc
*.pyo

# Nix
result
result-*
.direnv/


# Nix build outputs
result
result-*
# LazyVim generated state
config/nvim/lazyvim.json

# swe per-project memory (local, never commit)
.agents/memory/

# externalized skills installed per project (upstream-managed, never commit)
.agents/skills/
skills-lock.json
# exception: hand-authored project overrides ARE committed (e.g. git-workflow)
!.agents/skills/
.agents/skills/*
!.agents/skills/git-workflow/
!.agents/skills/git-workflow/**

# local-only skill artifacts: design specs + work-plan tracking (never commit)
.agents/brainstorm-studio/
status/

# reviewer HTML reports (local artifacts, never commit)
.reviews/

# browser automation state (playwright MCP, kept out if it returns)
.playwright-mcp/

# codegraph index (per-project, rebuilt via `codegraph init`)
.codegraph/

# config-level agent rules: track this one AGENTS.md despite the global ~/.gitignore
!config/opencode/AGENTS.md

# local worktrees (feature branches)
.worktrees/

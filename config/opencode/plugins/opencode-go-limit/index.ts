// Server side intentionally empty: all behavior lives in tui.tsx.
// No-op stub keeps the plugin directory valid for server-side discovery.
export default {
  id: "opencode-go-limit",
  setup() {},
};

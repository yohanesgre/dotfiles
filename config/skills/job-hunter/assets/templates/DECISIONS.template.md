# Decisions: your job search

A running log of meaningful per-session choices and their reasons. The agent appends to this file during your sessions; you can also add entries directly.

**Why this file exists:** so the agent (and future-you) can answer "wait, why did I skip Acme Corp again?" three months from now without re-litigating it. Decisions here feed pattern recognition in `LESSONS.md`.

**Privacy:** This file lives only in your workspace. It is never sent off-machine, never committed to the public skill repository. Add `.job-hunter/` to your `.gitignore` if you keep your workspace in git and don't want to commit it.

**Format per entry:**

```
## YYYY-MM-DD: Short imperative summary

**Context:** What was being decided
**Chosen:** What you (or the agent on your behalf) decided
**Reason:** The why, in your own words, this is what feeds learning
```

Decisions are immutable once written. If you change your mind, add a new entry that supersedes the old one rather than editing history.

---

<!-- Agent and user entries appended below this line -->
